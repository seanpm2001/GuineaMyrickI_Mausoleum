#include <stdio.h>
#include <string.h>
// Start of script
// Setting up your Mausoleum memorial
// I am not very good at programming in C. This may appear as bad code and may not compile correctly.
// Definitions
union tombSetup (1);
// Main
int main(void) {
    return tombType();
    return setupInstruct();
    break;
    exit;
} // End of main method
int tombType();
{ // May be merged with setupInstruct, keeping separate for now
  printf("What type of tomb are you setting up?");
  printf("\nOutdoor grave (with remains)\nOutdoor grave (withougt remains)");
  printf("\nMausoleum grave (with remains)\nCenotaph (no remains, mausoleum)");
  break;
}
int setupInstruct();
{
  // return tombType();
  printf("Setting up the tomb and memorial");
  printf("Name of the deceased");
  scanf(%%); // Not valid syntax, I need to learn input statements better. For now, I will leave this template
  printf("Name of the memorial (try not to name it the same as the person");
  scanf(%%); // See above (2 lines up)
  printf("General info\nHow old was the specimen?");
  scanf(%%); // See above
  printf("Of what species was the specimen?");
  scanf(%%); // ^
  printf("Birth date of the specimen");
  scanf(%%); // ^
  printf("Death date of the specimen");
  scanf(%%); // ^
  printf("Gender of the specimen (optional);
  scanf(%%); // ^
  // More information here
  printf("You have completed the information portion of the setup");
  break;
}
/* File info
* File version: `1 (Saturday, June 12th 2021 at 10:04 pm)
* File type: C Source file (*.c)
* Line count (including blank lines and compiler line): 53
*/
// This script is incomplete, and needs lots of work
// End of script
